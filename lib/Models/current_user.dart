class CurrentUser {
  String currentUserUID;
  String userName;
  String emailAddress;
  String photoUrl;
  String fullName;

  CurrentUser(
      {this.currentUserUID,
      this.emailAddress,
      this.fullName,
      this.photoUrl,
      this.userName});
}
